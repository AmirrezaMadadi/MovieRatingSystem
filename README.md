🎬 Movie Rating System (MRS)
این پروژه یک سیستم مدیریت و امتیازدهی به فیلم‌هاست که با معماری چندلایه (Layered Architecture) و بر پایه FastAPI طراحی شده است.

🚀 ویژگی‌های کلیدی
CRUD کامل فیلم‌ها: قابلیت ایجاد، مشاهده، ویرایش و حذف فیلم‌ها.

مدیریت ژانرها: دسته‌بندی فیلم‌ها با استفاده از رابطه Many-to-Many با جداول ژانر.

سیستم امتیازدهی: ثبت امتیاز برای هر فیلم و محاسبه لحظه‌ای میانگین نمرات.

فیلترینگ پیشرفته: جستجوی فیلم‌ها بر اساس نام، سال ساخت و ژانر به همراه Pagination.

سیستم Logging (فاز ۲): پیاده‌سازی لاگ‌های ساختاریافته (Structured Logging) در سطوح مختلف (INFO, WARNING, ERROR) برای رهگیری دقیق عملیات و دیباگ سریع‌تر.

🏗 معماری پروژه
پروژه از ساختار استاندارد زیر پیروی می‌کند:

Controllers: مدیریت Entry-pointهای API و اعتبارسنجی اولیه.

Services: پیاده‌سازی منطق تجاری (Business Logic) و مدیریت لاگ‌های فاز ۲.

Repositories: ارتباط مستقیم با دیتابیس با استفاده از SQLAlchemy ORM.

Models: تعریف ساختار جداول دیتابیس (PostgreSQL).

Schemas: تعریف ساختارهای داده‌ای Pydantic برای جابه‌جایی دیتا در شبکه.
